# Copy to .env.test and fill in. Used by the Playwright E2E suite.
# A throwaway account that exists on the target site.
TEST_EMAIL=test@example.com
TEST_PASSWORD=your-test-password

# Target site. Defaults to production if unset.
# BASE_URL=https://headline-blush.vercel.app
# BASE_URL=http://localhost:3000
